source "https://rubygems.org"

gem "jekyll", "~> 4.3" # Or the latest stable version
# gem "minima", "~> 2.5" # We are not using a theme, but this is how you'd add one
# Add other Jekyll plugins here if needed in the future

group :jekyll_plugins do
  gem "jekyll-feed"
  # other plugins...
end